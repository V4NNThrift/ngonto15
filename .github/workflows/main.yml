name: Yanzz Windows VPS System

on:
  workflow_dispatch:

jobs:
  windows-vps:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:
      - name: Konfigurasi Password Akun
        run: net user runneradmin Evan123456

      - name: Install Node.js & PM2 (Bot Environment)
        uses: actions/setup-node@v3
        with:
          node-version: '20'
      - run: npm install -g pm2

      - name: Install Python & Glances (Web Monitor)
        run: |
          pip install glances
          # Menjalankan Glances Web Server di latar belakang
          Start-Process -NoNewWindow -FilePath "glances" -ArgumentList "-w"

      - name: Setup Jaringan Tailscale
        run: |
          Invoke-WebRequest -Uri "https://pkgs.tailscale.com/stable/tailscale-setup-latest.exe" -OutFile "tailscale-setup.exe"
          Start-Process -FilePath ".\tailscale-setup.exe" -ArgumentList "/quiet" -Wait
          
          # Menghubungkan ke Tailscale
          & "C:\Program Files\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-windows-${env:GITHUB_RUN_ID}
          
          # Mengambil IP Tailscale
          $TS_IP = & "C:\Program Files\Tailscale\tailscale.exe" ip -4
          
          Write-Host ""
          Write-Host "=== INFORMASI AKSES VPS WINDOWS ==="
          Write-Host "IP RDP / Tailscale : $TS_IP"
          Write-Host "Username           : runneradmin"
          Write-Host "Password           : Evan123456"
          Write-Host "-----------------------------------"
          Write-Host "Web Monitor (Buka di Browser) :"
          Write-Host "http://$($TS_IP):61208"
          Write-Host "==================================="
          Write-Host ""

      - name: Menahan Server Tetap Hidup
        run: |
          Write-Host "Server berhasil berjalan. Hentikan workflow untuk mematikan."
          ping -t 127.0.0.1 > $null
